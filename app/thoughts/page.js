// Placeholder for the Blog page (lives at /thoughts per PROJECT_CONTEXT.md).
// Content comes in a later session — this just lets the nav tab navigate here.
export default function BlogPage() {
  return (
    <main className="min-h-[50vh] flex items-center justify-center px-4 text-center">
      <p className="text-ios-subtle">Blog (thoughts) — coming soon.</p>
    </main>
  );
}
